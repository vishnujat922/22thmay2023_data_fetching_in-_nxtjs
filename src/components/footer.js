import React from 'react'

const Footer = () => {
    return (
        <footer>
            <p class="text-white text-center">Resize the browser window to see how the content respond to the resizing.</p>
        </footer>
    )
}

export default Footer